#include <stdio.h> // stdio ย่อมาจาก standrad input output ใช้กับการแสดงผล printf scanf

int main() { //main คือจุดเริ่มต้นของโปรแกรม function นี้ส่งกลับเป็นค่าจำนวนเต็ม
    printf("Hello World\n");

    return 0; //หมายถึงโปรแกรมเสร็จเรียบร้อยแล้ว
}